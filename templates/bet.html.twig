{% block body %}
    {% include 'base.html.twig' %}

    <h1>Bet:</h1>
    <div class="centered_form">
        {{ form_start(betForm) }}
        {{ form_widget(betForm) }}

        <input type="submit" class="btn" value="Spin" />
        {{ form_end(betForm) }}
        <p>Win amount: {{ winAmount }} </p>
    </div>

{% endblock %}
